{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ "app_reset_password.title"|trans }}</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">{{ "app_reset_password.button"|trans }}</button>
    {{ form_end(resetForm) }}
{% endblock %}
