{% extends 'base.html.twig' %}

{% block body %}

    <div class="col-6 offset-3">
        <h1>
            {{ "app_reset_password.title"|trans }}
        </h1>
        {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">
            {{ "app_reset_password.button"|trans }}
        </button>
        {{ form_end(resetForm) }}
    </div>
{% endblock %}
